"use client";
import { Activity } from "lucide-react";
import type { Metric } from "./types";
export function MetricsCard({ metric, projectName, clusterName, onOpen }: { metric: Metric; projectName?: string; clusterName?: string; onOpen: (id: string) => void }) { return <article role="button" tabIndex={0} onClick={() => onOpen(metric.id)} onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onOpen(metric.id); } }} className="cursor-pointer rounded-3xl border p-5 shadow-[var(--shadow-sm)] transition-all hover:-translate-y-1 hover:shadow-[var(--shadow-md)]" style={{ backgroundColor: "var(--card)", borderColor: "var(--border)" }}><div className="flex gap-3"><span className="rounded-2xl p-3" style={{ color: "var(--primary)", backgroundColor: "color-mix(in srgb, var(--primary) 12%, transparent)" }}><Activity className="h-5 w-5" /></span><div className="min-w-0"><h2 className="truncate font-semibold">{metric.metricName}</h2><p className="mt-1 text-xs" style={{ color: "var(--muted-foreground)" }}>{metric.metricType} · {metric.resourceKind}</p></div></div><p className="mt-5 text-3xl font-semibold">{format(metric.value)} <span className="text-base font-medium" style={{ color: "var(--muted-foreground)" }}>{metric.unit}</span></p><dl className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><dt style={{ color: "var(--muted-foreground)" }}>Project</dt><dd className="mt-1 truncate">{projectName || metric.projectId}</dd></div><div><dt style={{ color: "var(--muted-foreground)" }}>Cluster</dt><dd className="mt-1 truncate">{clusterName || metric.clusterId}</dd></div></dl><p className="mt-4 border-t pt-4 text-xs" style={{ borderColor: "var(--border)", color: "var(--muted-foreground)" }}>{new Date(metric.timestamp).toLocaleString()}</p></article>; }
export const format = (v: number) => new Intl.NumberFormat(undefined, { maximumFractionDigits: 2 }).format(v);
