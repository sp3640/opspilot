"use client";
import { Search, X } from "lucide-react";
export function MetricsSearch({ value, onChange }: { value: string; onChange: (value: string) => void }) { return <div className="relative min-w-0 flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2" style={{ color: "var(--muted-foreground)" }} /><label className="sr-only" htmlFor="metrics-search">Search metrics</label><input id="metrics-search" value={value} onChange={(e) => onChange(e.target.value)} placeholder="Search metric names, resource kinds, or units" className="h-11 w-full rounded-2xl border bg-transparent py-2 pl-10 pr-9 text-sm" style={{ borderColor: "var(--border)" }} />{value ? <button type="button" className="absolute right-2 top-1/2 -translate-y-1/2 p-1" onClick={() => onChange("")}><X className="h-4 w-4" /></button> : null}</div>; }
