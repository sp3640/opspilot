"use client";
import { SlidersHorizontal } from "lucide-react";
import { METRIC_TYPES } from "@/lib/constants/metric";
import type { ClusterResponse } from "@/types/cluster-api";
import type { ProjectResponse } from "@/types/project-api";
import type { MetricFilters } from "./types";
const cls = "h-11 rounded-2xl border bg-transparent px-3 text-sm";
export function MetricsFilter({ filters, projects, clusters, onChange }: { filters: MetricFilters; projects: ProjectResponse[]; clusters: ClusterResponse[]; onChange: (x: Partial<MetricFilters>) => void }) { return <div className="flex flex-wrap items-center gap-2"><SlidersHorizontal className="hidden h-4 w-4 lg:block" /><select aria-label="Project" value={filters.projectId} onChange={(e) => onChange({ projectId: e.target.value })} className={cls} style={{ borderColor: "var(--border)" }}><option value="all">All projects</option>{projects.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select><select aria-label="Cluster" value={filters.clusterId} onChange={(e) => onChange({ clusterId: e.target.value })} className={cls} style={{ borderColor: "var(--border)" }}><option value="all">All clusters</option>{clusters.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select><input aria-label="Resource ID" value={filters.resourceId === "all" ? "" : filters.resourceId} onChange={(e) => onChange({ resourceId: e.target.value || "all" })} placeholder="Resource ID" className={cls + " w-32"} style={{ borderColor: "var(--border)" }} /><select aria-label="Metric type" value={filters.metricType} onChange={(e) => onChange({ metricType: e.target.value })} className={cls} style={{ borderColor: "var(--border)" }}><option value="all">All types</option>{METRIC_TYPES.map((x) => <option key={x}>{x}</option>)}</select><select aria-label="Time range" value={filters.timeRange} onChange={(e) => onChange({ timeRange: e.target.value as MetricFilters["timeRange"] })} className={cls} style={{ borderColor: "var(--border)" }}><option value="24h">Last 24 hours</option><option value="7d">Last 7 days</option><option value="30d">Last 30 days</option></select></div>; }
