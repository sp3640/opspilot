"use client";
import { BellRing, Clock } from "lucide-react";
import { AlertSeverityBadge } from "./alert-severity-badge";
import { AlertStatusBadge } from "./alert-status-badge";
import type { Alert } from "./types";
export function AlertCard({ alert, projectName, onOpen }: { alert: Alert; projectName?: string; onOpen: (id: number) => void }) { return <article role="button" tabIndex={0} onClick={() => onOpen(alert.id)} onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onOpen(alert.id); } }} className="cursor-pointer rounded-3xl border p-5 shadow-[var(--shadow-sm)] transition-all hover:-translate-y-1 hover:shadow-[var(--shadow-md)]" style={{ backgroundColor: "var(--card)", borderColor: "var(--border)" }}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><div className="flex gap-2"><BellRing className="mt-0.5 h-4 w-4 shrink-0" style={{ color: "var(--danger)" }} /><h2 className="truncate font-semibold">{alert.title}</h2></div><p className="mt-1 truncate text-xs" style={{ color: "var(--muted-foreground)" }}>{projectName || alert.projectId} · {alert.resourceType}: {alert.resourceId}</p></div><AlertSeverityBadge severity={alert.severity} /></div><p className="mt-4 line-clamp-2 min-h-10 text-sm" style={{ color: "var(--muted-foreground)" }}>{alert.description || "No message provided"}</p><div className="mt-4 flex flex-wrap gap-2"><AlertStatusBadge status={alert.status} /><span className="rounded-full px-2.5 py-1 text-xs font-medium" style={{ backgroundColor: "var(--muted)", color: "var(--muted-foreground)" }}>{alert.source}</span></div><div className="mt-4 flex items-center gap-1 border-t pt-4 text-xs" style={{ borderColor: "var(--border)", color: "var(--muted-foreground)" }}><Clock className="h-3.5 w-3.5" />Updated {new Date(alert.updatedAt).toLocaleDateString()}</div></article>; }
