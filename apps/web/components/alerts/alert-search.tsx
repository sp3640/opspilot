"use client";
import { Search, X } from "lucide-react";
export function AlertSearch({ value, onChange }: { value: string; onChange: (value: string) => void }) { return <div className="relative min-w-0 flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2" style={{ color: "var(--muted-foreground)" }} /><label htmlFor="alert-search" className="sr-only">Search alerts</label><input id="alert-search" value={value} onChange={(event) => onChange(event.target.value)} placeholder="Search alerts by title, message, or resource" className="h-11 w-full rounded-2xl border bg-transparent py-2 pl-10 pr-9 text-sm outline-none focus:border-[var(--primary)]" style={{ borderColor: "var(--border)" }} />{value ? <button type="button" className="absolute right-2 top-1/2 -translate-y-1/2 p-1" onClick={() => onChange("")} aria-label="Clear alert search"><X className="h-4 w-4" /></button> : null}</div>; }
