"use client";
import { Search, X } from "lucide-react";
export function ResourceSearch({ value, onChange }: { value: string; onChange: (value: string) => void }) { return <div className="relative min-w-0 flex-1"><Search aria-hidden className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2" style={{ color: "var(--muted-foreground)" }} /><label htmlFor="resource-search" className="sr-only">Search resources</label><input id="resource-search" value={value} onChange={(event) => onChange(event.target.value)} placeholder="Search resources by name, namespace, cluster, or ID" className="h-11 w-full rounded-2xl border bg-transparent py-2 pl-10 pr-9 text-sm outline-none focus:border-[var(--primary)]" style={{ borderColor: "var(--border)" }} />{value ? <button type="button" onClick={() => onChange("")} className="absolute right-2 top-1/2 -translate-y-1/2 p-1" aria-label="Clear resource search"><X className="h-4 w-4" /></button> : null}</div>; }
