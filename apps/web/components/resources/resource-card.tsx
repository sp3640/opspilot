"use client";
import { Boxes } from "lucide-react";
import { ResourceHealthBadge } from "./resource-health-badge";
import { ResourceKindBadge } from "./resource-kind-badge";
import { ResourceStatus } from "./resource-status";
import type { Resource } from "./types";
export function ResourceCard({ resource, projectName, onOpen }: { resource: Resource; projectName?: string; onOpen: (id: string) => void }) { return <article role="button" tabIndex={0} onClick={() => onOpen(resource.id)} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); onOpen(resource.id); } }} className="cursor-pointer rounded-3xl border p-5 shadow-[var(--shadow-sm)] transition-all hover:-translate-y-1 hover:shadow-[var(--shadow-md)]" style={{ backgroundColor: "var(--card)", borderColor: "var(--border)" }}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><div className="flex items-center gap-2"><Boxes className="h-4 w-4 shrink-0" style={{ color: "var(--primary)" }} /><h2 className="truncate font-semibold">{resource.displayName || resource.name}</h2></div><p className="mt-1 truncate text-xs" style={{ color: "var(--muted-foreground)" }}>{resource.namespace || "No namespace"} · {resource.cluster || "No cluster"}</p></div><ResourceKindBadge kind={resource.kind} /></div><div className="mt-5 flex flex-wrap gap-2"><ResourceStatus status={resource.status} /><ResourceHealthBadge health={resource.health} /></div><dl className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><dt style={{ color: "var(--muted-foreground)" }}>Project</dt><dd className="mt-1 truncate">{projectName || resource.projectId}</dd></div><div><dt style={{ color: "var(--muted-foreground)" }}>Provider</dt><dd className="mt-1 truncate">{resource.provider || "-"}</dd></div><div><dt style={{ color: "var(--muted-foreground)" }}>Region</dt><dd className="mt-1 truncate">{resource.region || "-"}</dd></div><div><dt style={{ color: "var(--muted-foreground)" }}>Updated</dt><dd className="mt-1">{new Date(resource.updatedAt).toLocaleDateString()}</dd></div></dl></article>; }
